export const userInfo = (state) => state.users.user
export const records = (state) => state.records.records
export const doctors = (state) => state.doctors.doctors
export const analyzes = (state) => state.analyzes.analyzes
export const scheduler = (state) => state.scheduler.scheduler
export const selectedDoctor = (state) => state.doctors.selectedDoctor
